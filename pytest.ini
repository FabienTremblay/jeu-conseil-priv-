[pytest]
markers =
    unit: tests unitaires rapides et déterministes
    it: tests d'intégration (flux complets, plus lents)
minversion = 8.0
testpaths = packages/cabinet/tests
pythonpath = .

# n’entre jamais dans ces répertoires
norecursedirs =
    .*          # .git, .venv, etc.
    build dist
    node_modules
    archives    # 🔸 ton dossier à exclure partout

# exclusions supplémentaires (même si pointés par erreur)
addopts =
    --ignore=packages/cabinet/skins/archives
    --ignore-glob="**/archives/*"

