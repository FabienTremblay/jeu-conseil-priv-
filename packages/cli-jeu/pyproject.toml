[project]
name = "cli-jeu"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["typer>=0.12"]  # ou rien + argparse
scripts = { avpol = "cli_jeu.main:app" }

[project.optional-dependencies]
dev = ["pytest"]

[tool.setuptools.packages.find]
where = ["src"]
